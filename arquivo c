#include<stdio.h>
int main(){
    int num;
    char nume[20];
    scanf("%d",&num);
    FILE *arq=fopen("arq.txt","w");
    for (int i = 0; i < num; i++)
    {
        fgets(nume,20,stdin);
        printf(' ');
    }
    fclose(arq);
    

}
